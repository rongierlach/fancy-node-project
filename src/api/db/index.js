// import config from 'config'
// import mongoose from 'mongoose'
// import promiseLibrary from 'bluebird'
// import schemas from './schemas'

// const {db: {uri}} = config

export default _ => {
  // const db = await mongoose.createConnection(uri) //, {promiseLibrary})

  // schemas.forEach((acc, [singularName, schema]) =>
  //   db.model(singularName, schema)
  // )

  // return db
}
